'use strict';

/**
 * position-in-preset router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::position-in-preset.position-in-preset');
