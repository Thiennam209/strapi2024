'use strict';

/**
 * position-in-preset service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::position-in-preset.position-in-preset');
