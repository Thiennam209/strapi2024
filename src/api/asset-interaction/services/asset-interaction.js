'use strict';

/**
 * asset-interaction service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::asset-interaction.asset-interaction');
